# Database workspace

- Runtime schema changes are owned by `backend/alembic/`.
- Future anonymized seed data belongs in `database/seed/`.
- Administrative scripts belong in `database/scripts/` and must not embed credentials.
- Database design documentation belongs in `docs/database/`.
